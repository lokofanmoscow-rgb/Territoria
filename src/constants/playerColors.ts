// Цвета фракций — насыщенные "флаговые" тона: у владельца провинция красится
// сплошным цветом (не полупрозрачным оверлеем поверх биома, как раньше) —
// биом остаётся виден только через значки рельефа. До 4 игроков за партию.
export const PLAYER_COLORS = ['#cf3a3a', '#2f6fb0', '#3f9142', '#e0b03c'] as const;

export type PlayerColor = (typeof PLAYER_COLORS)[number];
